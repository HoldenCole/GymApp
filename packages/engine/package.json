{
  "name": "@kanon/engine",
  "version": "0.1.0",
  "private": true,
  "description": "Kanon rule resolver: liturgical day-facts in, fasting obligation out. Calendar is imported, never re-derived.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~5.9.0",
    "vitest": "^3.2.0"
  }
}
