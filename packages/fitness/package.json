{
  "name": "@kanon/fitness",
  "version": "0.1.0",
  "private": true,
  "description": "Kanon fitness domain: energy targets (Mifflin-St Jeor, user-overridable), plans (bulk/cut/maintain), customizable splits, progress math.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kanon/engine": "*"
  },
  "devDependencies": {
    "typescript": "~5.9.0",
    "vitest": "^3.2.0"
  }
}
